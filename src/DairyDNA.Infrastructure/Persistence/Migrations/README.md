# Initial schema

Feature 000 uses `Database.EnsureCreatedAsync()` for local demo bring-up
(InMemory or SQL Server). Formal EF Core migrations are deferred to Feature 001
foundation work; model is defined in `DairyDnaDbContext`.
